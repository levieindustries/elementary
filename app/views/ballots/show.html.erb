<% @categories.each do |category| %>
  <%= react_component("BallotCategory",
    id: category.id,
    name: category.name,
    voted_nominee_id: nil,
    winning_nominee_id: nil,
    nominees: category.nominees) %>
<% end %>
